import React from 'react';
import { Button, Icon } from '@material-ui/core';

function HiddenLayerView() {
  return (
    <div className="HiddenLayerView">
      <Button variant="contained" color="primary">
        <Icon />
      </Button>
    </div>

  );
}

export default HiddenLayerView;
